name: poc_22_x11_gpu_presentation
description: POC-22 - X11 presentation matrix for PutImage, EGL/OpenGL, and Vulkan.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  dart_ui:
    path: ../..
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
