name: poc_01_win32_window
description: >
  POC-01: Provar que Dart pode controlar janelas Win32 sem wrapper C/C++
  usando dart:ffi com NativeCallable para WndProc callback.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  test: ^1.25.0
  lints: ^5.1.0
