name: poc_09_wayland
description: POC-09 — Wayland Client via FFI (Linux).
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0