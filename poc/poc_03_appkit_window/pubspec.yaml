name: poc_03_appkit_window
description: POC-03 - AppKit Window via Objective-C Runtime FFI
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  ffi: ^2.1.3
  poc_20_macos_three_backends:
    path: ../poc_20_macos_three_backends

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
