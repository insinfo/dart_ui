name: poc_05_com_direct2d
description: POC-05 — COM and Direct2D factory smoke test in pure Dart.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  ffi: ^2.1.3
  # win32 6.x requires Dart >= 3.10; 5.8 is the newest compatible line.
  win32: ^5.8.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
