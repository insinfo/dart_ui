name: poc_12_native_buffers
description: Benchmark Dart heap and FFI-native BGRA pixel buffers.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
