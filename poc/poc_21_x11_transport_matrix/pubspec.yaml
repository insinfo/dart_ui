name: poc_21_x11_transport_matrix
description: >-
  POC-21 — Compares libxcb FFI, dart:io Unix sockets, and libc socket FFI
  while sending equivalent X11 core protocol workloads.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
