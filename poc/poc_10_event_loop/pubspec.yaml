name: poc_10_event_loop
description: POC-10 — Cooperative Dart and Win32 event loop.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  ffi: ^2.1.3
  win32: ^5.8.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
